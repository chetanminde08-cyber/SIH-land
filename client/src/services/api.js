import axios from 'axios'; const api = axios.create({ baseURL:'/api' }); export const searchLocation = q => api.get('/land/search',{params:{q}}).then(r=>r.data); export const analyzeLand = body => api.post('/analysis',body).then(r=>r.data); export const uploadAttachments = form => api.post('/land/uploads',form,{headers:{'Content-Type':'multipart/form-data'}}).then(r=>r.data); export default api;
